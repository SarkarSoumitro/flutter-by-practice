🔴 Freezed ছাড়া (ম্যানুয়ালি JSON হ্যান্ডেল করলে)

ধরুন, আপনার কাছে একটা API রেসপন্স আছে:

{
  "id": 1,
  "title": "Inception",
  "poster_path": "/abc123.jpg",
  "overview": "A mind-bending thriller."
}


এখন এই ডেটা ধরতে হলে আপনাকে ম্যানুয়ালি একটা Dart class লিখতে হবে:

class Movie {
  final int id;
  final String title;
  final String? posterPath;
  final String? overview;

  Movie({
    required this.id,
    required this.title,
    this.posterPath,
    this.overview,
  });

  // JSON থেকে অবজেক্ট বানাতে
  factory Movie.fromJson(Map<String, dynamic> json) {
    return Movie(
      id: json['id'] as int,
      title: json['title'] as String,
      posterPath: json['poster_path'] as String?,
      overview: json['overview'] as String?,
    );
  }

  // অবজেক্ট থেকে JSON বানাতে
  Map<String, dynamic> toJson() {
    return {
      'id': id,
      'title': title,
      'poster_path': posterPath,
      'overview': overview,
    };
  }
}


👉 এখানে আপনি দেখতে পাচ্ছেন:

fromJson আর toJson আপনাকেই লিখতে হচ্ছে।

যদি ফিল্ড নাম একটু বড় হয়, ভুল হবার সম্ভাবনা থাকে।

১০–১৫টা ফিল্ড হলে কোড অনেক লম্বা হয়ে যায়।

পরে যদি নতুন কোনো ফিল্ড API তে আসে, তখন দুটো মেথডই আবার মডিফাই করতে হয়।

🟢 Freezed দিয়ে (Code Generation ব্যবহার করলে)

একই কাজ খুব ছোট কোডে করা যায়:

@freezed
class MovieModel with _$MovieModel {
  const factory MovieModel({
    required int id,
    required String title,
    @JsonKey(name: 'poster_path') String? posterPath,
    @JsonKey(name: 'overview') String? overview,
  }) = _MovieModel;

  factory MovieModel.fromJson(Map<String, dynamic> json) =>
      _$MovieModelFromJson(json);
}


এখানে:

fromJson আর toJson ম্যানুয়ালি লিখতে হচ্ছে না।

Freezed + JsonSerializable অটোমেটিক জেনারেট করে দেয়।

copyWith, ==, hashCode সবই অটো জেনারেটেড।

কোড অনেক ছোট, পড়তেও সহজ।

✅ কেন Freezed ছাড়া কষ্টকর হয়?

বারবার কোড লিখতে হয় (fromJson, toJson, copyWith, hashCode, equality ইত্যাদি)।

বড় API হলে ভুল করার সুযোগ বেশি (field নাম, টাইপ ইত্যাদি)।

ডুপ্লিকেট কোড হয় → প্রতিটি মডেলের জন্য একই boilerplate বারবার লিখতে হয়।

রক্ষণাবেক্ষণ (maintain) করা কঠিন → API তে নতুন ফিল্ড যোগ হলে আপনাকে ম্যানুয়ালি প্রতিটি জায়গায় পরিবর্তন করতে হয়।

✅ কেন Freezed সহজ?

অটোমেটিক কোড জেনারেট করে (fromJson, toJson, copyWith সব)।

কম কোড, কম ভুল।

ইমিউটেবল ডেটা মডেল পেয়ে যান।

স্টেট ম্যানেজমেন্টে সহজে ব্যবহার করা যায় (Bloc, Riverpod ইত্যাদিতে)।
